import SwiftUI

@main
struct ByCSLernclientApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
                .tint(Color(red: 0.04, green: 0.42, blue: 0.66))
        }
    }
}
